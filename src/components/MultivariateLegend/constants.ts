import type { Direction } from '~utils/bivariate';

export const DEFAULT_SCORE_DIRECTION: Direction = [['bad'], ['good']];
export const DEFAULT_BASE_DIRECTION: Direction = [['unimportant'], ['important']];
